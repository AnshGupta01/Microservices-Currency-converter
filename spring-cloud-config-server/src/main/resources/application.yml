server:
  port: 8888

spring:
  application:
    name: spring-cloud-config-server

  cloud:
    config:
      server:
        git:
          uri: file://Users/ansh/Data/Projects/Archive/microservices-new/git-localconfig-repo
          default-label: main